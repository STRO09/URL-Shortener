# ===== Database Configuration =====
spring.datasource.url=jdbc:mysql://localhost:3306/urlshortener?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=tfws.wow///POP()

# ===== JPA / Hibernate Configuration =====
spring.jpa.hibernate.ddl-auto=update
# 'update' \u2192 Hibernate automatically creates/updates tables from entities
spring.jpa.show-sql=true
# Logs SQL queries in console for debugging
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL57Dialect
# Ensures MySQL-compatible SQL generation

# ===== Server Configuration (Optional) =====
server.port=8080
# Default port is 8080; can be changed if needed
